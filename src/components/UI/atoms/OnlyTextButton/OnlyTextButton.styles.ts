import styled from 'styled-components';

import { ButtonWrapper } from '@atoms/Button/Button.styles';

const OnlyTextButtonWrapper = styled(ButtonWrapper)`
	background-color: transparent;
	border: none;
	color: #000000;
`;

export { OnlyTextButtonWrapper };
