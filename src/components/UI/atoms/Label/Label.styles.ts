import styled from 'styled-components';

const LabelWrapper = styled.p`
	color: #000000;

	font-family: NanumGothic;
	font-style: normal;
	font-weight: bold;
	font-size: 14px;
`;

export { LabelWrapper };
